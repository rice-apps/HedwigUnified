"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.sortOptions = exports.countThroughLinkResolver = exports.findManyThroughLinkResolver = exports.countResolver = exports.findManyResolver = exports.userLinkList = exports.userList = exports.userLinkTC = exports.userTC = exports.UserLinkType = exports.UserType = void 0;

var _graphqlCompose = require("graphql-compose");

var _graphql = require("graphql-compose/lib/graphql");

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(Object(source), true).forEach(function (key) { _defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

const UserType = new _graphql.GraphQLObjectType({
  name: 'User',
  fields: {
    id: {
      type: _graphql.GraphQLInt
    },
    name: {
      type: _graphql.GraphQLString
    },
    age: {
      type: _graphql.GraphQLInt
    },
    gender: {
      type: _graphql.GraphQLString
    }
  }
});
exports.UserType = UserType;
const UserLinkType = new _graphql.GraphQLObjectType({
  name: 'UserLink',
  fields: {
    id: {
      type: _graphql.GraphQLInt
    },
    type: {
      type: _graphql.GraphQLString
    },
    userId: {
      type: _graphql.GraphQLInt
    },
    otherUserId: {
      type: _graphql.GraphQLInt
    }
  }
});
exports.UserLinkType = UserLinkType;

const userTC = _graphqlCompose.schemaComposer.createObjectTC(UserType);

exports.userTC = userTC;

const userLinkTC = _graphqlCompose.schemaComposer.createObjectTC(UserLinkType);

exports.userLinkTC = userLinkTC;
const userList = [{
  id: 1,
  name: 'user01',
  age: 11,
  gender: 'm'
}, {
  id: 2,
  name: 'user02',
  age: 12,
  gender: 'm'
}, {
  id: 3,
  name: 'user03',
  age: 13,
  gender: 'f'
}, {
  id: 4,
  name: 'user04',
  age: 14,
  gender: 'm'
}, {
  id: 5,
  name: 'user05',
  age: 15,
  gender: 'f'
}, {
  id: 6,
  name: 'user06',
  age: 16,
  gender: 'f'
}, {
  id: 7,
  name: 'user07',
  age: 17,
  gender: 'f'
}, {
  id: 8,
  name: 'user08',
  age: 18,
  gender: 'm'
}, {
  id: 9,
  name: 'user09',
  age: 19,
  gender: 'm'
}, {
  id: 10,
  name: 'user10',
  age: 49,
  gender: 'f'
}, {
  id: 11,
  name: 'user11',
  age: 49,
  gender: 'm'
}, {
  id: 12,
  name: 'user12',
  age: 47,
  gender: 'f'
}, {
  id: 15,
  name: 'user15',
  age: 45,
  gender: 'm'
}, {
  id: 14,
  name: 'user14',
  age: 45,
  gender: 'm'
}, {
  id: 13,
  name: 'user13',
  age: 45,
  gender: 'f'
}];
exports.userList = userList;
const userLinkList = [{
  id: 1,
  type: 'likes',
  userId: 1,
  otherUserId: 2
}, {
  id: 2,
  type: 'dislikes',
  userId: 2,
  otherUserId: 1
}];
exports.userLinkList = userLinkList;
const filterArgConfig = {
  name: 'filter',
  type: new _graphql.GraphQLInputObjectType({
    name: 'FilterUserInput',
    fields: {
      gender: {
        type: _graphql.GraphQLString
      },
      age: {
        type: _graphql.GraphQLInt
      }
    }
  })
};
const filterEdgeArgConfig = {
  name: 'filter',
  type: new _graphql.GraphQLInputObjectType({
    name: 'FilterNodeEdgeUserInput',
    fields: {
      edge: {
        type: new _graphql.GraphQLInputObjectType({
          name: 'FilterNodeEdgeEdgeUserInput',
          fields: {
            type: {
              type: _graphql.GraphQLString
            }
          }
        })
      },
      node: {
        type: new _graphql.GraphQLInputObjectType({
          name: 'FilterNodeNodeEdgeUserInput',
          fields: {
            gender: {
              type: _graphql.GraphQLString
            },
            age: {
              type: _graphql.GraphQLInt
            }
          }
        })
      }
    }
  })
};

function filterUserLink(link, filter = {}) {
  let pred = true;

  if (filter.type) {
    pred = pred && link.type === filter.type;
  }

  return pred;
}

function filterUser(user, filter = {}) {
  let pred = true;

  if (filter.gender) {
    pred = pred && user.gender === filter.gender;
  }

  if (filter.id) {
    if (filter.id.$lt) {
      pred = pred && user.id < filter.id.$lt;
    }

    if (filter.id.$gt) {
      pred = pred && user.id > filter.id.$gt;
    }
  }

  if (filter.age) {
    if (filter.age.$lt) {
      pred = pred && user.age < filter.age.$lt;
    }

    if (filter.age.$gt) {
      pred = pred && user.age > filter.age.$gt;
    }
  }

  return pred;
}

function filteredUserList(list, filter = {}) {
  return list.slice().filter(o => filterUser(o, filter));
}

function sortUserList(list, sortValue = {}) {
  const fields = Object.keys(sortValue);
  list.sort((a, b) => {
    let result = 0;
    fields.forEach(field => {
      if (result === 0) {
        if (a[field] < b[field]) {
          result = sortValue[field] * -1;
        } else if (a[field] > b[field]) {
          result = sortValue[field];
        }
      }
    });
    return result;
  });
  return list;
}

function prepareFilterFromArgs(resolveParams = {}) {
  const args = resolveParams.args || {};

  const filter = _objectSpread({}, args.filter);

  if (resolveParams.rawQuery) {
    Object.keys(resolveParams.rawQuery).forEach(k => {
      filter[k] = resolveParams.rawQuery[k];
    });
  }

  return filter;
}

const findManyResolver = _graphqlCompose.schemaComposer.createResolver({
  name: 'findMany',
  kind: 'query',
  type: UserType,
  args: {
    filter: filterArgConfig,
    sort: new _graphql.GraphQLEnumType({
      name: 'SortUserInput',
      values: {
        ID_ASC: {
          value: {
            id: 1
          }
        },
        ID_DESC: {
          value: {
            id: -1
          }
        },
        AGE_ASC: {
          value: {
            age: 1
          }
        },
        AGE_DESC: {
          value: {
            age: -1
          }
        }
      }
    }),
    limit: _graphql.GraphQLInt,
    skip: _graphql.GraphQLInt
  },
  resolve: resolveParams => {
    const args = resolveParams.args || {};
    const {
      sort,
      limit,
      skip
    } = args;
    let list = userList.slice();
    list = sortUserList(list, sort);
    list = filteredUserList(list, prepareFilterFromArgs(resolveParams));

    if (skip) {
      list = list.slice(skip);
    }

    if (limit) {
      list = list.slice(0, limit);
    }

    return Promise.resolve(list);
  }
});

exports.findManyResolver = findManyResolver;
userTC.setResolver('findMany', findManyResolver);

const countResolver = _graphqlCompose.schemaComposer.createResolver({
  name: 'count',
  kind: 'query',
  type: _graphql.GraphQLInt,
  args: {
    filter: filterArgConfig
  },
  resolve: resolveParams => {
    return Promise.resolve(filteredUserList(userList, prepareFilterFromArgs(resolveParams)).length);
  }
});

exports.countResolver = countResolver;
userTC.setResolver('count', countResolver);

function getThroughLinkResolver(list, filter) {
  const nodeFilter = filter ? filter.node : {};
  const edgeFilter = filter ? filter.edge : {};
  return list.map(link => _objectSpread({}, link, {
    node: userList.find(u => u.id === link.otherUserId && filterUser(u, nodeFilter))
  })).filter(l => !!l.node && filterUserLink(l, edgeFilter));
}

const findManyThroughLinkResolver = _graphqlCompose.schemaComposer.createResolver({
  name: 'findManyThroughLink',
  kind: 'query',
  type: UserType,
  args: {
    filter: filterEdgeArgConfig,
    limit: _graphql.GraphQLInt,
    skip: _graphql.GraphQLInt
  },
  resolve: async resolveParams => {
    const args = resolveParams.args || {};
    const {
      limit,
      skip
    } = args;
    let list = userLinkList.slice();

    if (skip) {
      list = list.slice(skip);
    }

    if (limit) {
      list = list.slice(0, limit);
    }

    return getThroughLinkResolver(list, args.filter);
  }
});

exports.findManyThroughLinkResolver = findManyThroughLinkResolver;
userTC.setResolver('findManyThroughLink', findManyThroughLinkResolver);

const countThroughLinkResolver = _graphqlCompose.schemaComposer.createResolver({
  name: 'count',
  kind: 'query',
  type: _graphql.GraphQLInt,
  args: {
    filter: filterEdgeArgConfig
  },
  resolve: async resolveParams => {
    const args = resolveParams.args || {};
    return getThroughLinkResolver(userLinkList.slice(), args.filter).length;
  }
});

exports.countThroughLinkResolver = countThroughLinkResolver;
userTC.setResolver('countThroughLink', countThroughLinkResolver);
const sortOptions = {
  ID_ASC: {
    value: {
      id: 1
    },
    cursorFields: ['id'],
    beforeCursorQuery: (rawQuery, cursorData) => {
      if (!rawQuery.id) rawQuery.id = {};
      rawQuery.id.$lt = cursorData.id;
    },
    afterCursorQuery: (rawQuery, cursorData) => {
      if (!rawQuery.id) rawQuery.id = {};
      rawQuery.id.$gt = cursorData.id;
    }
  },
  AGE_ID_DESC: {
    value: {
      age: -1,
      id: -1
    },
    cursorFields: ['age', 'id'],
    beforeCursorQuery: (rawQuery, cursorData) => {
      if (!rawQuery.age) rawQuery.age = {};
      if (!rawQuery.id) rawQuery.id = {};
      rawQuery.age = {
        $gt: cursorData.age
      };
      rawQuery.id = {
        $gt: cursorData.id
      };
    },
    afterCursorQuery: (rawQuery, cursorData) => {
      if (!rawQuery.age) rawQuery.age = {};
      if (!rawQuery.id) rawQuery.id = {};
      rawQuery.age = {
        $lt: cursorData.age
      };
      rawQuery.id = {
        $lt: cursorData.id
      };
    }
  }
};
exports.sortOptions = sortOptions;